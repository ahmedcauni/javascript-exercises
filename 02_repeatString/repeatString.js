const repeatString = function(word,num) {
    let wordrep=""
    for(let i=0;i<num;i++){
        wordrep+=word;
    }
    return wordrep;
};

// Do not edit below this line
module.exports = repeatString;
